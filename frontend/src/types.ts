export interface DailyLog {
  date: string;
  dayOfWeek: string;
  temp: string;
  humidity: string;
  feeding: string;
  waterChange: boolean;
  cleaning: boolean;
  poop: boolean;
  shed: boolean;
  notes: string;
}
